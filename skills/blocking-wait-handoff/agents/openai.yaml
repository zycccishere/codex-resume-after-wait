interface:
  display_name: "Blocking Wait Handoff"
  short_description: "Continue Codex after long-running processes"
  default_prompt: "Use $blocking-wait-handoff to watch this already-running process and continue its durable owner task when it exits."
